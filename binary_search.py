def busca_binaria(lista,number_search,kick):
    init = 0
    end = len(lista)
    medium = (init+end) // 2
    sorted(lista)
    if kick == number_search:
        return medium
    elif kick > number_search:
        medium = () 
    elif kick < number_search:
        medium = ()